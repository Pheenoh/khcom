# MVP pack build manifest (US asset_gfx). Policy B: sources/bins not committed.
schema_version: 1
id: asset_gfx
version: us
class: pack
unit: asset_gfx
rom:
  start: 0x084E0B04
  end: 0x0886AD18
  size: 3711508
inventory: config/asset_inventory_us_gfx.yaml
path_mode: built
extract_path: assets/us/084E0B04-0886AD18.bin
build_path: build/us/assets/asset_gfx.bin
source_root: assets/us/src/gfx/asset_gfx
asm_slice: asm/us/asset_gfx.s
asm_built: build/us/asm/asset_gfx.s
tools:
  gbagfx: tools/gbagfx/gbagfx
  packer: tools/gfx/asset_gfx_pack.py
  layout: tools/gfx/asset_gfx_layout.py
sha256_slice: abd74f58e092cdb9c28f71d42d934645ce95cd714cb7108b2d6632f0101f7312
status: remux_built_green
tools_extra:
  dump: tools/gfx/asset_gfx_dump.py
  extent: tools/movie_assets.py
  movie_re: tools/gfx/movie_open_re.py
encode_re:
  status: in_progress_not_matching
  matching_path: demux_remux_copy_through
  tool_modes: [summary, analyze, probe, experimental-audio]
  video:
    i_frame: 16x16 tiles of 8 bytes (c0,c1,pattern); upsample in ARM
    p_filter_header: u16 sec0 + u16 sec1 + three bitstreams
    filter: P decode then RGB555 blur blob
  audio_codec_id_2:
    mapped: 2-bit ADPCM to 8-bit PCM; predictor/index packing known
    step_table: gUnk_09D6D1E4 (IMA-like; diffs at 84,85)
    experimental_encode: gated; not bit-identical
  blockers: [P bitstream commands, retail I pattern encode, audio bit-identical]
build:
  mode: demux_remux_copy_through
  note: >
    Built path demuxes MovieOpen streams + pad/MovieSub/MOVIE from the extract
    slice into source_root, then remuxes byte-identical. Encoder RE is follow-on;
    probe/experimental-audio stay outside check.sh.
regional:
  movie_payloads_identical: [us, jp, eu]
  built_mode: [us, jp, eu]
  matrix:
    us: built
    jp: built (pack unit asset_gfx_at_084D4390; tiny head unit stays slice)
    eu: built
